var request = require('request');
var pkginfo = require('../package');

Entity = {};
module.exports = Entity;

var basePath = pkginfo.baasBasePath;

Entity.get = function(params, callback) {
    callback();
};

Entity.update = function(params, callback) {
    callback();
};

Entity.create = function(params, callback) {
    callback();
};

Entity.delete = function(params, callback) {
    callback();
};

Entity.search = function(params, callback) {
    callback();
};