<div class="consent-page-wrapper">
  <div class="consent-page">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h2>Access authorization</h2>
      </div>
      <div class="panel-body">
        <div class="consent-content">
          <p>Application
            <strong>{{application}}</strong> is requesting your permission to share the following details</p>
          {{#if scopes}}
            <ul class="list-group">
              {{#each scopes}}
                <li class="list-group-item">{{this}}</li>
              {{/each}}
            </ul>
          {{else}}
            <h3>The application does not require any permissions.</h3>
          {{/if}}
          {{#if tnc}}
          <div class="panel panel-default">
            <div class="panel-body">
              <div>I agree to the <a href="{{tnc}}" class="tnc-link" target="_blank">Terms and conditions</a></div>
            </div>
          </div>
          {{/if}}
          <p>Please press
            <strong>Allow</strong> to authenticate else press
            <strong>Cancel</strong>
          </p>
          <div class="consent-button">
            <form role="form" action="{{{basePath}}}/consent" method="post">
              <button type="submit" name="allow" value="allow" class="btn btn-primary grass-btn">Allow</button>
              <button type="submit" name="deny" value="deny" class="btn btn-primary grass-btn">Cancel</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
